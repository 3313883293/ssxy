v0.678 更新内容：陨石卡改为从屏幕左上角斜俯冲飞向主目标

## 背景

用户要求「让陨石卡从屏幕左上角斜飞过来」——取代 v0.676 的垂直坠落（主目标正上方落下）。

## 改动（v0.677 → v0.678）

1. **js/SkillSystem.js** `_meteorFallShow`：
   - 起点改为 **arena 左上角视口外（-90, -90）**；
   - 运动学：**横向线性横移 + 纵向 ease-in 重力加速**（俯冲感），飞行时长 520ms，自旋动画保持；
   - 终点不变（主目标位置上方），撞击后爆散 / 震荡波 / 距离分级颤动 / 全屏震动全部保留。
2. **js/globals.js**：GAME_VERSION → v0.678。

## 自动适配（零改动）

- 机制结算、距离颤动、音效、目标侧爆炸时序均不变；
- 仅演出路径调整。

## 验证

适配 `check-v0674-newanim.js` C2a：陨石卡创建后立即查询其 left/top 为负值（左上角视口外起点），名字为「陨石」；C2b/C2c 不变。
- 回归：check-v0673-caojiameng（27 项）+ check-v0674-newanim（8 项）+ check-v0677-savefix（7 项）。
