v0.293 更新内容：剑痕停留留痕 + 十二连加成伤害/恶加伤 50→100

## 1. 十二连剑痕修复「闪一下就没」（用户反馈「你这剑痕对吗」）

v0.292 及之前的剑痕 0.3 秒劈出后直接淡出消失（animSlash 100% 帧 opacity 0 + forwards），每道金线一闪而过，目标身上留不下任何「被砍」的痕迹，且十二道方向随机交错，观感像乱划。

按用户确认（保持随机交错刀痕，只修停留）：
- 每道剑痕改为 **0.15s 短促劈出**（scaleX 0→1 + 高亮闪光），劈出后**刀痕留在目标身上**（深浅随机 opacity 0.65~1.0）
- 十二刀依次劈完（约 1.1s，间隔 100ms），与角色闪回同时（1.45s）给全部刀痕加 `.fade` 类**一起淡出**（0.3s），随后统一移除 DOM
- CSS：animSlash 改为劈出停留版（100% 帧 opacity 用 var(--opacity) 保留痕迹）；新增 .skill-slash.fade（transition opacity 0.3s）

## 2. 十二连·剑斩邪祟数值调整（用户指定）

- 加成伤害（每枚硬币）：50 → **100**（技能变为 `300 + 100×硬币(12)`）
- 恶加伤（【伤害时】目标每层「恶」额外伤害）：50 → **100**
- 同步更新「恶」词条描述（转化伤害 50→100；每层无视 50 防御不变）
- 伤害公式面板（battleActions.js 渲染）与技能效果行（glossary.js 动态生成）自动跟随，无需改文案

注：技能数据核对——Skill 构造函数参数顺序为 (name, spCost, baseDamage, bonusDamage, coinCount, attackRange)，十二连 SP 消耗 800、基础伤害 300、加成伤害 50（现已改 100）、12 硬币、距 3。
