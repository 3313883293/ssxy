v0.305 更新内容：剑气放大 + 曲率再小 + 垂直中心与角色对齐

用户需求：「曲率稍微再小一点，剑气可以大一点，并且保持与角色高度的中心一致」

## 改动（SkillSystem.js：_swordWave 形状与定位）

1. **剑气放大**：SVG 110×80 → **140×100**（viewBox 等比 140×100），弧线整体放大；线宽 10/4 → **12/5** 保持观感比例
2. **曲率再小**：控制点 `Q 36 40` → `Q 30 50`（新画布），凸出 12px → **9px**，放大后视觉占比更平
3. **垂直中心对齐**（修复 + 用户指定）：原 top = 角色卡片中心 y，但剑气高 80 导致视觉中心偏下 40px——现改为 `top = 角色中心 − 剑气高/2`，剑气垂直中心与角色卡高度中心严格一致

其余不变：直线高加速弹道、命中反解同步、dir<0 镜像、380ms 淡出回收。

## 验证

- verify-anim 全量通过（圆弧形断言、加速飞行、800ms 回收 + 伤害同步）
- verify-idle 全量通过
