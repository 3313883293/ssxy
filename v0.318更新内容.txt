v0.318 更新内容：关卡中途退出键

用户需求：战斗中加一个退出键，打一半可以退出。

## 行为（已与用户确认）

点「🚪 退出」→ 确认 → 回主界面，自动存档保留（主界面「继续战斗」从最近回合开始接着打）——相当于暂停/临时退出，不丢进度。

## 改动

**index.html**：`#pageBattle` 的 battle-header 里、读档按钮旁新增「🚪 退出」按钮（红色，onclick="exitBattle()"）。

**battleFlow.js**：新增 `exitBattle()`——confirm 确认后复用现有 `backToTitle()`（已实现回主界面 + 保留存档 + 刷新「继续战斗」按钮 + 清内存战斗态 + 丢弃旧纪元异步）。教程关战斗中退出同样安全（showPage 会清理教学弹窗）。

## 验证

- check-v318-exit.js：进入战斗 → 出现「🚪 退出」按钮 → 点击弹确认 → 接受 → 回主界面且「继续战斗」按钮可见 → 点继续战斗能恢复战斗
- 回归：verify-v316（21 项）在 v0.318 服务器上全过
