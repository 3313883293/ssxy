v0.292 更新内容：修复十二连闪砍遮挡目标卡片

按用户反馈：「你全挡住了我看什么？」——v0.291 的十二连闪移偏移只有目标卡片宽度的 30%，角色闪过去后几乎完全重叠盖在目标卡片上（zIndex 50 + 1.2 秒停留），目标的血量/状态/剑痕全被挡住。

## 修复：闪过去贴边站，不重叠

- _flashToTarget 的偏移量改为「半个目标卡片宽 + 半个角色卡片宽 + 14px 间隙」→ 角色卡片右缘刚好贴住目标卡片左缘（或反向），砍的时候目标卡片完全可见，剑痕照常划在目标身上
- 闪移/闪回动画与停留时长不变

验证：playwright UI 完整流程（点技能按钮→选目标→确认）实测 transform 变化——闪过去贴边（约 130px 位移）→ 1.45s 后闪回原位 → 清理干净；截图确认两卡片无重叠。
