v0.302 更新内容：剑气本体改为「圆弧形月牙光刃」（弹道回归直线高加速）

用户澄清：「不是弹道为圆弧，是剑气本身为圆弧」——v0.301 误把弹道做成拱形弧线，用户要的是剑气这个光刃本身是圆弧形状，飞行仍是直线加速。

## 改动（SkillSystem.js：_swordWave 形状改造 + style.css 注释）

**圆弧形剑气（v0.302）**：
- 光刃形状：竖直线（v0.300）→ **二次贝塞尔月牙弧** `M 12 12 Q 80 40 12 68`（viewBox 110×80）——两端尖、弧背凸向飞行方向、凸出约 34px（曲率平缓）；双层 stroke 保留（蓝刃 10 + 白芯 4）+ drop-shadow 发光
- **弹道回归直线**：y 恒定不拱、无姿态旋转，transform = `translate(x, 0)`；朝左飞时附加 `scale(-1, 1)` 镜像让弧背始终朝飞行方向（注意 CSS transform 顺序：translate 在 scale 左侧先应用，位移不受镜像影响）
- **高加速运动学保留**（用户上轮要求）：x(t) = v0·t + ½a·t²（初速 = 平均速/3、末速 ≈ 5/3），setInterval 16ms 逐帧驱动；命中时刻运动学反解（t_hit ≈ 0.8·T，速度最快时）受伤反馈 + 蓝色冲击波同步；T=380ms 后淡出回收

## 验证

- verify-anim 第 5 节新增「剑气本体为圆弧形」断言（path 的 d 含 Q 贝塞尔），全量通过：圆弧形 ✓、100ms x 位移 26.3px 加速飞行 ✓、800ms 回收 + 伤害数字同步 ✓
- verify-idle 全量通过
